version: '3.8'

services:
  redis:
    image: redis:latest
    ports:
      - "6379:6379"

  marks_agent:
    build: .
    depends_on:
      - redis
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - DB_PATH=data/students_profiles.db
    volumes:
      - ./data:/app/data
